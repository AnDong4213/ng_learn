import { MyStepperNextDirective } from './my-stepper-next.directive';

describe('MyStepperNextDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStepperNextDirective();
    expect(directive).toBeTruthy();
  });
});
