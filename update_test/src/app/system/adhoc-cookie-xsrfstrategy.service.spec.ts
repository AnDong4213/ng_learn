import { TestBed, inject } from '@angular/core/testing';

import { AdhocCookieXSRFStrategy } from './adhoc-cookie-xsrfstrategy.service';

describe('AdhocCookieXSRFStrategy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdhocCookieXSRFStrategy]
    });
  });

});
